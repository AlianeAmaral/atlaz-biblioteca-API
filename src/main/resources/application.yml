spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/atlaz_biblioteca
    username: postgres
    password: admin12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

data:
  mongodb:
    uri: mongodb://localhost:27017/biblioteca_midia

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

server:
  port: 8081